<h1 class="text-2xl font-bold mb-4">Self Advances</h1>

<table class="min-w-full text-sm border border-gray-200">
  <thead>
    <tr class="bg-gray-100">
      <th class="px-4 py-2 border-b text-left">num_term</th>
    </tr>
  </thead>
  <tbody>
    <% @self_advances.each do |sa| %>
      <tr>
        <td class="px-4 py-2 border-b"><%= sa.num_term %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "← Torna alla Home", root_path, class: "text-blue-600 hover:underline mt-6 inline-block" %>